# dashboard

Next.js + TypeScript frontend (CLAUDE.md §23).

**Empty in Phase 0 by design.** The dashboard is Phase 12 — it is built only
after the analytical engines it is meant to display exist. Scaffolding a
frontend now would create maintenance surface with nothing to render.

Node.js v24.21.0 and npm 11.19.0 are already installed on this machine, but
are not on the `PATH`; see README.md → *Known environment gaps*.
